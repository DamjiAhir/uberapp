const dbConnnection = require("../database/dbconnect");
const userModel = require("../models/userModel");

module.exports.registerUser = async (req, res, next) => {
  dbConnnection();
  try {
  } catch (error) {}
};
